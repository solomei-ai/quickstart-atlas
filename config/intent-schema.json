{
  "mode": "structured",
  "schema": {
    "type": "object",
    "required": [
      "wildlifeInterest"
    ],
    "additionalProperties": false,
    "properties": {
      "wildlifeInterest": {
        "type": "object",
        "required": [
          "summary",
          "traits",
          "title"
        ],
        "additionalProperties": false,
        "properties": {
          "title": {
            "type": "string",
            "description": "A short title naming what the visitor has explored this session. Where several distinct subjects were explored, reflect the range rather than picking one."
          },
          "traits": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Up to 5 keywords naming traits, habitats, classifications, or eras the visitor seems drawn to (e.g. predator, nocturnal, ocean, mammal, ice age)."
          },
          "summary": {
            "type": "string",
            "description": "One sentence describing what the visitor is curious about. If they have followed more than one distinct subject this session, name each of them in the order they were explored, rather than only the most recent. Address the visitor as \"You\"."
          }
        }
      }
    }
  }
}
